spring:
  datasource:
    url: jdbc:mysql://localhost:3306/realtors?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Delta007@
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
  server:
    port: 8081
  app:
    security:
      enabled: true